/** Body copy: font size only — line-height inherits from the page root slider. */
export const BODY_TEXT_CLASS = "text-[0.875rem] leading-inherit";

/** Smaller supporting copy that should still follow body line-height. */
export const BODY_TEXT_SM_CLASS = "text-[0.75rem] leading-inherit";
